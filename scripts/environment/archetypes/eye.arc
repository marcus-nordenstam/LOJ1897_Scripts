# Two eyes per human; capacity covers human-npc (4096) + human-player (256).
archetype "eye" (cap 8704) (per obs) (non-occluder)
{
    (spatial bounds)
    # PR-evi-A 2026-05-25 - per-body-part evidence attrs.
    (attr "blemishes")
}
