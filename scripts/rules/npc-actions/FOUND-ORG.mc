; DORMANT - this lane never ran; revived on the form deeds / articles with its own gauntlet.
;; ----------------------------------------------------------------------------
;; FOUND-ORG ?org-kind ?head-role - found an organisation of ?org-kind, @self at its head
;; as ?head-role. The founding sequence is found-org-seq's, as for a business (FOUND) or a
;; club (FOUND-CLUB); what differs is who decides it and what head the org gets.
;; ----------------------------------------------------------------------------

;(npc-action {@self FOUND-ORG ?org-kind ?head-role}:?fo-rel
;  (tar [k org])
;  (aux [k job])
;  (motor body legs)
;  (duration (seconds 90 min))
;  (effects
;    (found-org-seq ?org-kind ?head-role)
;    (set-outcome ?fo-rel /succ)))
