; verdict - one row per burial: the body's objective death-cause against the reading the
; burier took off the corpse. A kill cause read as natural is a covert murder.
(chronicle verdict
  (columns (deceased name) (cause kind) (violent bool)))
