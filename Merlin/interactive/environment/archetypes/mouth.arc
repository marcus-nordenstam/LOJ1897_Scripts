# One mouth per human; capacity scales with human-npc cap (4096).
archetype "mouth" (cap 4096) (per obs) (non-occluder)
{
    (spatial bounds)
    # PR-evi-A 2026-05-25 - per-body-part evidence attrs.
    (attr "blemishes")
}
