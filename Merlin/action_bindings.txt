# action_bindings.txt
#
# Maps Merlin actions to GRYM resources. Loaded once at startup.
# The Merlin action name [SECTION] is the key.
# Motor comes from the Merlin ontology, not from this file.
#
# Keys:
#   expression = <filename.esp>           expression from library (implies generic expression proc_anim)
#   proc_anim = <name>                    specialized proc_anim (tell, lookat, blink)
#   update = <name>                       specialized update function (walk, run, walkto, turn, etc.)
#   anim = <exact name>                   animation (gender-neutral, without .gra extension)
#   anim.female = <exact name>            female-specific animation
#   anim.male = <exact name>              male-specific animation
#   anim_flags = <flags>                  loop, pingpong, reset, randomize_start, backwards
#   alias = <MERLIN_ACTION>               reuse another action's animation
#   state = <flags>                       state bits to set: walking, running, sitting, standing, telling
#   base_speed = <float>                  base speed (anim playback, turning rad/s, IK blend rate)
#   base_duration = <float>              duration in seconds for timed actions (0 or omitted = indefinite)
#   attempt_duration = <float>           timeout for condition actions before fail
#   movement_speed = <float>             translation m/s at base_speed=1.0 (walk/run only)
#   nav_turn_speed = <float>             navigation turning speed rad/s (walk/run only)
#   player_transition_speed = <float>    player-only crossfade rate override

[TELL]
proc_anim = tell
update = tell_speech
state = telling
base_speed = 0.8

[ASK]
alias = TELL
proc_anim = tell
update = tell_speech
state = telling
base_speed = 0.8

[LOOK_AT]
proc_anim = lookat
update = lookat
base_speed = 1.0

[BLINK]
proc_anim = blink
update = blink
base_speed = 2.0

[SMILE]
expression = smile_neutral.esp
base_speed = 1.0

[CRY_EXPR]
expression = Scene_01_CryingLady_X_008.esp
base_speed = 1.0

[WALK]
anim.female = Motion_Fem_Walk_Normal_01
anim.male = Motion_Male_Walk_Normal_01
anim_flags = loop reset
update = walk
state = walking
base_speed = 1.0
movement_speed = 0.1
nav_turn_speed = 8.0
player_transition_speed = 8.0

[WALK_TO]
alias = WALK
update = walkto
state = walking
base_speed = 1.0
movement_speed = 0.1
nav_turn_speed = 8.0

[RUN]
anim.female = Motion_Fem_Run_01
anim.male = Motion_Male_Run_01_
anim_flags = loop reset
update = run
state = running
base_speed = 1.0
movement_speed = 0.5
nav_turn_speed = 10.0

[JUMP]
update = jump
base_speed = 8.0

[SIT]
anim = Motion_Uni_Sitting_Talking_01
anim_flags = loop reset
state = sitting
base_speed = 1.0

[STAND]
alias = SIT
update = stand
anim_flags = backwards
base_speed = 1.0

[CHAT]
anim.female = Motion_Female_Stand_Conversation_Acting_01
anim.male = Motion_Male_Stand_Listen_01
anim_flags = loop reset
state = standing
base_speed = 1.0

# Arms

[RIGHT_ARM_OUT]
anim = right_arm_out
anim_flags = loop reset
base_speed = 1.0

[LEFT_ARM_OUT]
anim = right_arm_out
anim_flags = loop reset
base_speed = 1.0

# Hands

[LEFT_GRASP]
update = grasp
base_speed = 8

[RIGHT_GRASP]
anim = right_hand_grip
anim_flags = loop reset
update = grasp
base_speed = 8

[RIGHT_ARM_BURY_FACE]
anim = right_hand_bury_face
anim_flags = loop reset
base_speed = 1.0

[LEFT_ARM_BURY_FACE]
anim = left_hand_bury_face
anim_flags = loop reset
base_speed = 1.0

# Head

[HANG_HEAD]
anim = hang_head
anim_flags = loop reset
base_speed = 1.0

[OPEN_JAW]
proc_anim = open_jaw
base_speed = 2.0

[TILT_BACK_HEAD]
anim = right_drink
anim_flags = loop reset
base_speed = 0.5



[STOW]
update = stow
base_speed = 4

[PUT]
update = put
base_speed = 1.0

[POUR]
update = pour
base_speed = 1.0


[LEFT_REACH_FOR]
proc_anim = reachfor
update = reachfor
base_speed = 0.3
attempt_duration = 5.0

[RIGHT_REACH_FOR]
proc_anim = reachfor
update = reachfor
base_speed = 0.3
attempt_duration = 5.0


[RIGHT_ARM_DRINK]
anim = right_drink
anim_flags = loop reset
base_speed = 0.5

[LEFT_ARM_DRINK]
anim = right_drink
anim_flags = loop reset
base_speed = 0.5


[RIGHT_HAND_DRINK]
anim = right_drink
anim_flags = loop reset
base_speed = 0.5

[LEFT_HAND_DRINK]
anim = right_drink
anim_flags = loop reset
base_speed = 0.5

[TURN_TO]
update = turnto
base_speed = 2.0

[MIRROR]
update = mirror
base_speed = 6.0

# -------------------------------------------------------------------------------
# IDLE is engine-internal (not a Merlin action). Settings here are used as input
# to init_idle_bindings() which creates one idle per motor at startup.
[IDLE]
anim.female = Motion_Female_Idle_01
anim.male = Motion_Male_Idle_02
anim_flags = pingpong reset randomize_start
state = standing
base_speed = 1.0
