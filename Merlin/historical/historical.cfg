# Historical pre-sim config. Only non-default values listed.
#
# start_population caps the founder pass (authored + procedural fill). 0 means
# "fill every residential building", which today exceeds Merlin's pool sizes
# in this scripts-set, so we cap at a number known to fit while we keep
# wiring up subsystems.
seed             = 4242
start_year       = 1700
end_year         = 1897
start_population = 20
