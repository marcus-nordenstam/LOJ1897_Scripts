# Meta-entities for performable (e.g. observable) activities (bartending, clerking, etc.)
archetype "activity" [256] /meta
{
    "isa"           kind                            /kind   /passivePercept
    "at"            entity  "structure"|"space"             /passivePercept
}
