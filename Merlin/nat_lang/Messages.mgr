
:MSG

# Formulaic phrases first

"'('  $form(FORM) ')'"
$form(FORM)
.

# Bare kind (e.g. "a beer")

"'(' $kind(INDEF_KIND) ')'"
( msg $kind(INDEF_KIND) )
.

# Then non-formulaics:

"'('  $st(ST) ')'"
( msg $st(ST) )
.

"'('  $np(NP) ')'"
( msg $np(NP) )
.
